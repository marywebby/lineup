<%= form_with(model: product_ingredient) do |form| %>
  <% if product_ingredient.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_ingredient.errors.count, "error") %> prohibited this product_ingredient from being saved:</h2>

      <ul>
        <% product_ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :ingredient_id, style: "display: block" %>
    <%= form.text_field :ingredient_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
